services:
  traefik:
    image: traefik:v2.10
    container_name: traefik
    restart: unless-stopped
    ports:
      - "0.0.0.0:443:443"
      - "0.0.0.0:4443:4443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ../certs:/etc/traefik/certs:ro
      - ca-certs:/ca-certs:ro
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic.yml:/etc/traefik/dynamic.yml:ro
    environment:
      - GLA1V3_DOMAIN=${GLA1V3_DOMAIN}
    networks:
      - gla1v3-net
    depends_on:
      - ca-service

  backend:
    build: ../backend
    container_name: backend
    restart: unless-stopped
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - ALLOW_INSECURE_DEFAULTS=${ALLOW_INSECURE_DEFAULTS:-false}
      - GLA1V3_DOMAIN=${GLA1V3_DOMAIN}
      - AGENT_WHOAMI_TOKEN=${AGENT_WHOAMI_TOKEN}
      - OPENSEARCH_URL=${OPENSEARCH_URL}
      - OPENSEARCH_AUTH_METHOD=${OPENSEARCH_AUTH_METHOD}
      - OPENSEARCH_USER=${OPENSEARCH_USER:-}
      - OPENSEARCH_PASS=${OPENSEARCH_PASS:-}
      - JWT_SECRET=${JWT_SECRET}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - INTERNAL_TOKEN=${INTERNAL_TOKEN}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=gla1v3
      - DB_USER=gla1v3_api
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ../certs:/app/certs:ro
      - ../agents-go:/agents-go:ro
      - agent-builds:/app/builds
      - wazuh-logs:/var/ossec/logs:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - ca-service
      - redis
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=infra_gla1v3-net"
      - "traefik.http.routers.backend-api.rule=Host(`api.gla1v3.local`)"
      - "traefik.http.routers.backend-api.entrypoints=websecure"
      - "traefik.http.routers.backend-api.tls=true"
      - "traefik.http.routers.backend-api.middlewares=rate-limit@file,request-size-limit@file"
      - "traefik.http.routers.backend-api.service=backend-api-svc"
      - "traefik.http.services.backend-api-svc.loadbalancer.server.port=3000"
      - "traefik.http.routers.c2-beacon.rule=Host(`c2.gla1v3.local`) && PathPrefix(`/beacon`)"
      - "traefik.http.routers.c2-beacon.entrypoints=c2"
      - "traefik.http.routers.c2-beacon.tls=true"
      - "traefik.http.routers.c2-beacon.tls.options=mtls@file"
      - "traefik.http.routers.c2-beacon.middlewares=pass-client-cert@file,rate-limit@file,request-size-limit@file"
      - "traefik.http.routers.c2-beacon.service=c2-svc"
      - "traefik.http.services.c2-svc.loadbalancer.server.port=3001"
      - "traefik.http.routers.c2-health.rule=Host(`c2.gla1v3.local`) && Path(`/health`)"
      - "traefik.http.routers.c2-health.entrypoints=websecure"
      - "traefik.http.routers.c2-health.tls=true"
      - "traefik.http.routers.c2-health.service=backend-api-svc"
    networks:
      - gla1v3-net
      - wazuh-net

  frontend:
    build: ../frontend
    container_name: frontend
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard.rule=Host(`dashboard.gla1v3.local`) || Host(`gla1v3.local`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.tls=true"
      - "traefik.http.services.dashboard.loadbalancer.server.port=5173"
    networks:
      - gla1v3-net

  ca-service:
    build: ../ca-service
    container_name: ca-service
    restart: unless-stopped
    environment:
      - CERT_DIR=${CERT_DIR:-/certs}
    volumes:
      - ca-certs:/certs
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ca-service.rule=Host(`ca.gla1v3.local`)"
      - "traefik.http.routers.ca-service.entrypoints=websecure"
      - "traefik.http.routers.ca-service.tls=true"
      - "traefik.http.services.ca-service.loadbalancer.server.port=3003"
    networks:
      - gla1v3-net

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis-data:/data
    networks:
      - gla1v3-net
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  ca-certs:
    driver: local
  agent-builds:
    driver: local
  redis-data:
    driver: local
  wazuh-logs:
    name: wazuh_wazuh-logs
    external: true

networks:
  gla1v3-net:
    external: true
    name: infra_gla1v3-net
  wazuh-net:
    driver: bridge